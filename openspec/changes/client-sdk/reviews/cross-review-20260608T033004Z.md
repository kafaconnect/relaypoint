# Cross-review (round 2 / re-review): client-sdk (+ signaling-core)

- **When:** 20260608T033004Z  · **Builder:** claude(+subagent) · **Reviewers:** codex, agy (independent, read-only)
- **Verdict:** agy=PASS_WITH_FINDINGS, codex=BLOCKED → **consolidated BLOCKED** (take stricter; codex blockers are real)

## Prior-round findings: ALL confirmed RESOLVED by both reviewers
warm-transfer-in-core, recording-vocab-drift, command-idempotency, SDK-state-observability, MediaAdapter answer/reneg, capability-split, transfer→InteractionHandle, video/tracks/state, opaque metadata — all RESOLVED.

## New / regression findings this round
### BLOCKER
1. [REGRESSION] envelope/wire drift — client-sdk 'verbatim envelope' omits `command_id?/caused_by?`; SDK API is camelCase (commandId/causedBy/objectRef) with NO normative camelCase↔snake_case wire mapping. (codex; agy flagged the omission as MEDIUM) — fix: sync envelope + add normative wire-naming note.
2. [NEW] recording state legality unspecified — no RecordingState/transitions: start-before-consent, consent-denied-vs-start, stop/upload retry, capture failure, terminal/error. (codex) — add RecordingState + idempotent failure scenarios.
### HIGH
3. [NEW] interaction lifecycle state machine not enumerated — core says 'explicit state machine' but never lists states/transitions. (codex+agy)
4. [NEW] transfer lifecycle incomplete — only target-accept; target reject/RONA/expiry/cancel/fail + old-leg-revoke race blank; client claims full transfer vocabulary (cold/warm/consult/cancel/complete/failed) NOT present in core events. (codex+agy) — spec the cold-transfer lifecycle facts or drop the claim.
5. [NEW] gap-replay failure path — JetStream down / replay can't fill the gap: SDK behavior unspecified. (codex+agy)
6. [NEW] ticket expiry/invalid/replay — Media-IAM validation failure + SDK recovery/state unspecified. (codex)
7. [NEW] concurrent interaction-level commands — two agents transfer at once, or double recording-start: no resolver/CAS/state-guard. (agy)
### MEDIUM
8. [REGRESSION] offer `expired` terminal has no trigger scenario vs timed_out_rona; client design subject-table still omits some control terminals. (codex)
9. [NEW] renegotiation field drift — core `negotiation_id`/generation vs SDK `generation` only; createAnswer/renegotiation task lacks a `// @spec:` tag. (codex)

## Open design questions (codex)
- SDK camelCase API vs snake_case wire — intentional? where is the mapping normative? (lean: yes, add a normative mapping note)
- Full transfer vocabulary = core event types now, or conceptual only? (lean: spec the COLD-transfer lifecycle as core facts; warm/consult stay deferred)
- What SDK state surfaces on expired-ticket / replay-failure? (lean: typed fatal -> setup_failed/media_failed or a degraded 'stale' state + reconnect)

## Fidelity: faithful to B1/B3, C1/C3, D, the 3 user points; gaps are completeness, not direction.

---
## Raw: codex
```
VERDICT: BLOCKED

PRIOR_FINDINGS:
- Warm transfer in M1 PARTIAL — `signaling-core` spec/design/tasks now say cold/blind only (`signaling.call.transfer`), but `openspec/changes/signaling-core/proposal.md:17-20` still says `cold/warm transfer`.
- Recording vocabulary RESOLVED — core defines consent requested/granted/denied, started/stopped, upload completed/failed with `retention_policy`/`recorder_id`/`object_ref`/`failure_reason`; capture is profile-specific (`signaling.recording.*`).
- Command idempotency/ack PARTIAL — core has `command_id`, dedup, `caused_by`, rejection result (`signaling.cmd.idempotent-command-id`), but SDK “verbatim” envelope omits `command_id?/caused_by?` and uses camelCase without wire mapping.
- SDK observable states RESOLVED — `RelayPointClient.state`, `CallController.state`, `on("state")`, getToken failure, credential fetch failure, refresh-before-expiry are specified (`clientsdk.connection.*`, `clientsdk.creds.*`, `clientsdk.call.state-observable`).
- MediaAdapter answer/renegotiation RESOLVED — `createAnswer`, renegotiation, stale generation discard, `onRemoteTrack` specified (`clientsdk.call.*`, `clientsdk.webrtcp2p.*`).
- Recording capability split RESOLVED — `supportsLocalRecording` and `supportsServerRecording` are distinct (`clientsdk.capability.declared-flags`).
- Medium refinements PARTIAL — terminals/schema/tenantId/media_profile mostly fixed, but client design subject table still lists only cancel/withdraw/accepted_elsewhere for control and core has no `expired` transition scenario.
- Transfer moved to InteractionHandle RESOLVED — `InteractionHandle.transfer(target)` specified (`clientsdk.transfer.cold-only`).
- Video/media kind/tracks/state RESOLVED — media kind, track exposure, camera upgrade, call state specified (`clientsdk.call.media-kind`, `track-exposure`, `toggle-mic-camera`).
- Opaque interaction context RESOLVED — `interaction.context.updated` and `InteractionHandle.metadata` specified (`signaling.interaction.context-updated`, `clientsdk.handle.metadata-observable`).

RUBRIC:
- R1 GAP — Built machines: connection specified; offer terminals enumerated but core lacks `expired` trigger; call mostly specified; interaction only asserts “explicit machine” without full states/transitions; recording has facts but no legal state machine; transfer happy path only, no reject/RONA/cancel/fail exits.
- R2 GAP — Lifecycle edges: connection abort/fail/reconnect covered; offer reject/reconnect/double-accept/withdraw covered, expiry blank; command retry covered; call glare/ICE/renegotiation/cancel/media-fail covered; ticket expiry, replay failure, MediaRecorder capture failure, transfer target non-accept are blank.
- R3 GAP — Actor matrix: client/router log resolved by authority; accept/accept and accept/withdraw by CAS; retry/original by `command_id`; live/replay by sequence pause; offer/offer glare by perfect negotiation; blanks remain for consent/start/deny, upload retry, and transfer old-leg/new-target/cancel races.
- R4 GAP — Failure list: token expiry, getToken fail, Media-IAM down, command retry, router crash, media failure covered; ticket expiry, JetStream replay fail, MediaRecorder capture fail/retry, and upload retry semantics not fully defined.
- R5 GAP — Authority inventory: `.log` non-writer, own-author `.signal`, no vendor secrets, tenant isolation covered; signaling-session ticket scope exists, but expiry/replay/invalid-ticket behavior is not specified.
- R6 GAP — Contract inventory: subjects mostly match; core envelope has `command_id?/caused_by?` while client “verbatim” envelope omits them; SDK public fields are camelCase without normative wire mapping; core says `negotiation_id`/generation while SDK exposes generation only; transfer vocabulary claimed but not in core event contract.
- R7 GAP — Delivery inventory: `command_id` dedup, `caused_by`, `event_id` dedup, sequence ordering, gap replay covered; blanks are replay failure, same `command_id` with different payload, and exact wire mapping for command/result correlation.
- R8 GAP — Scenario/task inventory matches by ID, but behavioral tasks without `// @spec` remain (`MediaAdapter createAnswer + renegotiation`, transfer vocabulary); `openspec validate --strict` could not be verified because `openspec` is unavailable in PATH.

FINDINGS:
- [BLOCKER] `client-sdk` spec/design — SDK claims verbatim core envelope but omits `command_id?/caused_by?` and uses `commandId/causedBy/objectRef/failureReason` without normative snake_case wire mapping — define exact wire envelopes/results and explicit public-to-wire mapping — R6/R7 — [REGRESSION]
- [BLOCKER] `signaling-core` + `client-sdk` recording artifacts — recording vocabulary is fixed, but recording state legality is still unspecified: consent denied vs start, start before consent, stop/upload retry, capture failure, terminal/error states — add `RecordingState`/transition scenarios and idempotent failure handling — R1/R3/R4 — [NEW]
- [HIGH] `signaling-core` interaction lifecycle — core says “explicit state machine” but does not enumerate all interaction states/transitions beyond invalid-after-ended, abandoned, orphaned, offline/left — add full interaction state diagram and scenarios — R1/R2 — [NEW]
- [HIGH] transfer artifacts — cold transfer specifies only target accept; target reject/RONA/expiry/cancel/fail and old-leg revoke race are blank, while client claims full transfer vocabulary not present in core events — specify transfer lifecycle/facts or remove the vocabulary claim — R1/R3/R6 — [NEW]
- [HIGH] delivery/failure specs — gap replay has no failure path when JetStream replay cannot fill the gap or stream is unavailable — specify SDK state/error/recovery and idempotent resume behavior — R4/R7 — [NEW]
- [HIGH] credential flow — signaling-session ticket expiry/invalid/replay behavior is not specified, despite short-lived ticket claims — define Media-IAM validation failure and SDK recovery/state transition — R4/R5 — [NEW]
- [MEDIUM] offer lifecycle — `expired` is a terminal but core has no scenario/task defining its trigger vs `timed_out_rona`; client design subject table still omits several control terminals — add core expiry scenario and update the table — R1/R6 — [REGRESSION]
- [MEDIUM] WebRTC renegotiation contract — core requires `negotiation_id`/generation, SDK only exposes `generation`; task for `createAnswer`/renegotiation has no `// @spec` tag — align fields and trace task to scenario — R6/R8 — [NEW]

NEW_GAPS_FROM_REVISION:
- Envelope/wire-field drift introduced by the command_id/caused_by fix.
- Recording facts are now aligned, but recording state/failure legality remains blank.
- Cold-transfer correction leaves stale proposal text and no non-accept transfer lifecycle.
- Ticket expiry and replay failure remain unspecified.
- Renegotiation field naming drift: `negotiation_id`/generation vs generation only.

DECISION_FIDELITY:
- B1/B3 mostly faithful: ticket exchange and no vendor secrets are covered; ticket expiry/replay is missing.
- C1/C3 partial: design/spec/tasks are cold/blind-only and consultative is app-level, but core proposal is stale and transfer failure/vocabulary is not normative.
- D partial: recording fact vocabulary and local/server capability split are faithful; recording state/recovery is incomplete.
- User points faithful: transfer moved to `InteractionHandle`, video/tracks/state added, opaque `context` surfaced.
- Refinements partial: offer terminals and media_profile mostly fixed; command correlation semantics fixed but exact envelope/field mapping is not.

QUESTIONS:
- Is the SDK public API intentionally camelCase while the wire contract remains snake_case? If yes, where is that mapping normative?
- Is full transfer vocabulary intended as core event types now, or only future conceptual notes?
- What exact SDK state should surface on expired ticket and replay failure?
```
## Raw: agy
```
```text
VERDICT: PASS_WITH_FINDINGS
PRIOR_FINDINGS:
- signaling-core warm transfer: RESOLVED — `signaling-core/specs/signaling-core/spec.md` (`signaling.call.transfer`) restricts M1 to cold/blind.
- recording fact vocabulary drift: RESOLVED — `signaling-core/specs/signaling-core/spec.md` (`signaling.recording.consent-facts`) defines facts exactly.
- command idempotency/ack missing: RESOLVED — `signaling-core/specs/signaling-core/spec.md` (`signaling.cmd.idempotent-command-id`) defines `command_id` dedup and `caused_by`.
- SDK state machines not specified: RESOLVED — `client-sdk/specs/client-sdk/spec.md` (`clientsdk.connection.state-observable`, `clientsdk.call.state-observable`) exposes states and `getToken`/credential failure behaviors.
- MediaAdapter lacked answer/renegotiation: RESOLVED — `client-sdk/design.md` `MediaAdapter` interface includes `createAnswer`, `createRenegotiationOffer`, `onRemoteTrack`.
- recording capability conflated local vs server: RESOLVED — `client-sdk/design.md` `MediaCapabilities` splits `supportsLocalRecording` and `supportsServerRecording`.
- MEDIUM offer-control/LogEvent/expired/media_profile: RESOLVED — addressed in `signaling-core/specs/signaling-core/spec.md` and `client-sdk/design.md`.
- User-raised transfer/video/metadata: RESOLVED — `InteractionHandle.transfer`, video opts in `start()`, and `metadata` surfaced as opaque.

RUBRIC:
R1 State machines: GAP — connection (disconnected→connecting→connected→reconnecting→closed/failed), offer (offered→ringing→accepted/rejected/cancelled/withdrawn/accepted_elsewhere/timed_out_rona/expired/no_responder_fast_rona) built OK. Call (idle→setup_offered→answered→ice_connecting→connected→renegotiating/held/transferring/reconnecting→connected) built but GAP: `transferring` exits not explicitly defined (does success tear down the leg into `ended`?). Interaction: GAP — states not explicitly enumerated (only events listed). Recording: N/A (facts only, capture is not core). Transfer: GAP — state machine not defined.
R2 Lifecycle edges: GAP — abort/timeout/retry/reconnect/duplicate/out-of-order/expiry/empty checked. Token expiry, IAM down, command idempotency built OK. GAP: no absolute timeout defined for `send(command)` ultimate abort if retries loop.
R3 Races/concurrency: GAP — Double-accept (CAS on offer_id), accept/withdraw cross (CAS on offer_id), Glare (perfect-negotiation polite/impolite) built OK. GAP: No CAS or resolver defined for concurrent transfer commands or concurrent recording start/stop commands.
R4 Failure/recovery: GAP — Token expiry (max lifetime+reconnect), Media-IAM down (typed error -> setup_failed), ticket expiry (proactive refresh), getToken fail (backoff -> auth_failed), command retry (command_id idempotency), MediaRecorder/upload fail (recording.upload.failed fact) built and defined. GAP: "replay fail" is not defined (behavior if JetStream gap-replay absolute failure occurs).
R5 Authority/security/tenancy: OK — SDK never writes .log, own-author signal (interaction.<id>.signal.<userId>), ticket scope/expiry (short-lived interaction-scoped), no vendor secrets (MediaCredentialProvider port), tenant isolation (tenant-prefixed subjects + payload match) built and enforced.
R6 Contract/SSoT integrity: GAP — Envelope drift. `signaling-core/specs/signaling-core/spec.md` defines `{ schema, event_type, event_id, sequence, occurred_at, tenant_id, actor_id, medium, media_profile?, command_id?, caused_by?, ref_id?, data }`. `client-sdk/specs/client-sdk/spec.md` verbatim envelope omitted `command_id` and `caused_by`.
R7 Idempotency/ordering/delivery: OK — command_id dedup (router drops duplicate), caused_by correlation (fact matches command_id), event_id dedup (Nats-Msg-Id), sequence ordering (router-assigned monotonic), gap-replay (pause live apply and replay) built and defined.
R8 Completeness/DoD: OK — Every Requirement maps to a stable-id Scenario; every Scenario maps to a `// @spec:` task. Decisions B1/B3 (auth callout / ticket exchange), C1/C3 (cold transfer / app consult), D (recording) + video/metadata/transfer user points built and covered.

FINDINGS:
- [HIGH] `signaling-core/specs/signaling-core/spec.md` — Interaction state machine states are not enumerated. The spec states it must follow an "explicit state machine", but only lists event types (started/assigned/transferred/abandoned/ended). — Enumerate the exact Interaction states and transitions. — R1 — NEW
- [HIGH] `signaling-core/specs/signaling-core/spec.md` — Call `transferring` state exit transitions are missing. The state machine string implies it returns to `connected`, but a successful transfer "tears down" the leg, implying `ended`. — Explicitly define the success and failure exits for `transferring`. — R1 — NEW
- [HIGH] `signaling-core/specs/signaling-core/spec.md` — Concurrent commands for interaction-level actions (transfer, recording start/stop) lack a resolver/CAS. If two agents blindly transfer at the same time, or both start recording, race conditions occur. — Define the CAS or authority behavior for concurrent interaction commands. — R3 — NEW
- [MEDIUM] `client-sdk/specs/client-sdk/spec.md` — The verbatim envelope definition in the SDK spec omitted the `command_id` and `caused_by` fields added to the signaling-core spec in the revision. — Sync the envelope string to exactly match signaling-core. — R6 — REGRESSION
- [MEDIUM] `client-sdk/specs/client-sdk/spec.md` — Absolute failure of JetStream gap-replay is not specified (e.g., if JetStream is down). — Define whether the SDK surfaces a fatal error, disconnects, or loops indefinitely on replay fail. — R4 — NEW

NEW_GAPS_FROM_REVISION:
- Envelope drift in `client-sdk/specs/client-sdk/spec.md` due to the `command_id` revision in core.
- Missing explicit state enums for Interaction and missing transitions for Call `transferring` (made prominent by the detailed state machine audits prompted by revisions).

DECISION_FIDELITY:
Faithful. B1/B3 (auth callout & ticket exchange), C1/C3 (cold transfer & consult app pattern), and D (recording best-effort vs compliance) are implemented exactly as decided. User points (video tracks, interaction handle transfer, opaque metadata) are strictly adhered to.

QUESTIONS:
- For `interaction.transfer.requested` concurrency, should the router reject the second request if the call is already in the `transferring` state?
```
```
