# Cross-review: agent-feed-admin-operation-observer

- Date: 2026-07-30
- Builder: codex
- Independent reviewer: agy
- Base: `b36a42814bdc12e5b74b9d3d991729fbce5499e0`
- Reviewed commit: `9afefba`
- Verdict: `PASS_WITH_FINDINGS`

## Scope

The review covered the exact signed capability profile, additive grant construction, tenant
isolation, subscribe-only enforcement, compatibility with existing agents and the dev HMAC
grammar, Desk AsyncAPI coordination, scenario traceability, and infrastructure scope.

## Findings

- LOW: `AO-01-capability-grant.md` remained `in_progress` after implementation and verification.
  Resolved by marking the task done and recording the final verification evidence.

## Outcome

No BLOCKER, HIGH, or MEDIUM findings. The reviewer confirmed:

- only a verified Desk EdDSA token can retain the exact capability;
- the grant is limited to `tenant.<verified-tenant>.admin.operation.*.changed`;
- same-tenant subscribe succeeds while cross-tenant subscribe and all browser publish attempts
  fail closed;
- ordinary and unknown-profile agents keep their existing feed grant without gaining the
  operation subject;
- the dev HMAC grammar remains unchanged;
- all three OpenSpec scenarios have automated `// @spec:` coverage;
- no new persistence, environment variable, or architectural abstraction was introduced.

The change is ready after the task metadata correction and final repository gates.
